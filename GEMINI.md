# Gemini Context

This project wraps a Postgres database to provide an in-order sequence of events, known as a stream, which may be
queried.  There are several different transport schemes available.

The `docs` directory should be considered the primary source of truth for project documentation.

The AI is instructed to test any recommended code changes before presenting them.

AI may not use the `git` command.
